// Package execpolicy provides exact-path allowlist checks for execution
// interception. It receives configured absolute local-command paths and
// feeds callers with boolean decisions about whether a candidate path
// should run locally.
package execpolicy
