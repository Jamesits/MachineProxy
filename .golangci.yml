version: "2"

run:
  build-tags:
    - backend_ssh
    - backend_docker

linters:
  exclusions:
    rules:
      # Ignore unchecked errors from deferred Close() calls.
      - linters:
          - errcheck
        source: 'defer .*\.Close\(\)'
